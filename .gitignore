.env
cache/
__pycache__/
*.pyc
covenant_specs.json
docmap.json
dataset/real/

# submission.json IS the deliverable and is tracked on purpose, together with the two
# variants it is chosen from — they are the evidence behind that choice.
